#server:
#  port: 1212


#management:
#  server:
#    port: 8082

management:
  endpoints:
    web:
      exposure:
        include: "*"
        exclude:
         - health
         - beans        
  endpoint:
     shutdown:
       enabled: true

#springfox:
# documentation:
#  auto-startup: false

spring:
 datasource:
  url: jdbc:mysql://localhost:3306/zensar
  username: root
  password: root
 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
  properties:
   hibernate:
    format_sql: true
    
